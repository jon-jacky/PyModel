# Make symlinks so we can just invoke pmt ... etc. not pmt.py ... etc.
# Invoke this script just once in each PyModel installation pymodel directory
# For Unix-like systems because pmt.py etc. all begin with this "shebang line":
#!/usr/bin/env python
# We keep the .py on program names so they are easy to run in Windows
ln -s pma.py pma
ln -s pmg.py pmg
ln -s pmt.py pmt
ln -s pmv.py pmv
ln -s trun.py trun
ln -s wsgirunner.py wsgirunner

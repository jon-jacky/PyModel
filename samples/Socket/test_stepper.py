"""
test_stepper - msocket tests with asynchronous stepper using select
"""

cases = [
    ('Just the model, no scenarios or configurations',
     'pmt -n 10 -c 6 msocket observables -i stepper'),
    ]
